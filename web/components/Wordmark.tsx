/** "AgentWorks" with the "Works" half in Settle Blue. */
export function Wordmark({ style }: { style?: React.CSSProperties }) {
  return (
    <span className="wordmark" style={style}>
      Agent<span className="w2">Works</span>
    </span>
  );
}
